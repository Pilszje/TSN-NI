# TSN-NI
A TSN network interface currently implementing 802.1Qbv
This repo is in an early stage and very barebones. Currently, the HDL folder contains just the (system) verilog description to build the network interface. To interface with Ethernet networks a MAC has to be provided.
The C folder contains the driver code for interfacing with the network interface hardware.
